# NOTE: This bot now uses 'config.json' for all settings and wallets.
# You can still use .env for these two basic keys if config.json doesn't exist.

PRIVATE_KEY=PASTE-MAIN-WALLET-KEY-HERE
HELIUS_API_KEY=PASTE-HELIUS-KEY-HERE

# All other settings (SOL amounts, Sub-wallets, Jito tips, etc.) 
# are managed via config.json or the Web Dashboard (http://localhost:8080)
